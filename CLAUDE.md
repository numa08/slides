# CLAUDE.md

このリポジトリは、勉強会・LT（裏テック無尽など）の**発表資料置き場**です。
原稿（Markdown）とスライド生成スクリプトを管理し、`pnpm slides` で PowerPoint（.pptx）を生成します。

## ディレクトリ規約（重要）

**イベントごとに 1 ディレクトリ**を作り、その中で資料を作ります。命名は開催年月の `YYYYMM/`。

```
YYYYMM/
  ├─ <原稿>.md          # 発表原稿（日本語）
  ├─ build_slides.js    # そのイベント専用のスライド生成スクリプト
  └─ <参考資料>         # PDF など（必要に応じて .gitignore）
```

- **各イベントディレクトリに `build_slides.js` を置く**こと。これが本リポジトリの中心的な規約。
- イベントごとにテーマ・デザインは異なってよい（むしろ変えていく方針）。共通化を急がず、まずはそのイベント専用に作り込む。
- 既存イベントの実例: [202607/](202607/)（FT8 の話。ダークテーマ＝ウォーターフォール/夜空モチーフ）。新規作成時のリファレンスとして読むとよい。

## スライド生成

```bash
pnpm slides <path-to-md>
#   例) pnpm slides 202607/FT8の話.md  ->  202607/FT8の話.pptx
```

- ランナーは [scripts/gen-slides.js](scripts/gen-slides.js)。渡された md と**同じディレクトリの `build_slides.js`** を実行し、**同じ場所に同名の `.pptx`** を出力する。
- `build_slides.js` は `process.argv[2]`（md パス）から**出力先・出力名のみ**を決める。スライドの内容はスクリプト内に直接定義する（md は自動パースしない）。
- 生成は**決定論的**（毎回同一の出力）。乱数を使う装飾は固定シードにすること。

## build_slides.js を書くときの約束

- **pptxgenjs** を使用（`require("pptxgenjs")`、ルートの `node_modules` から解決）。`LAYOUT_WIDE`（13.33 × 7.5 inch）を基本にしている。
- **フォント**: 本文 `Yu Gothic`、コード/数式/数値 `Consolas`。閲覧は WSL2 ではなく **Windows の PowerPoint** で行うため、Windows 標準フォント前提で選ぶ。
- **特殊グリフに注意**: フォント未対応で豆腐（□）になりうる記号（⊕ ⇄ ≡ など）は避け、`+` `→` `·` など無難なものを使う。
- 202607 の `build_slides.js` には共通ヘルパー（`base` / `heading` / `card` / `waterfall`）と配色・定数（`C` / `F` / `M`）が定義済み。新規イベントでも流用しやすいので、まず一読する。
- **座標は手計算**で重なり・はみ出しを避ける（後述のとおり LibreOffice が無く自動レンダー確認ができないため）。

## QA（レンダラが無い前提）

- この環境に **LibreOffice は入れない**（見た目の最終確認は人間が PowerPoint で行う）。
- エージェント側でできる確認:
  - **内容チェック**: 生成した `.pptx` を `unzip` し、`ppt/slides/slideN.xml` から `<a:t>` のテキストを抽出して、文言・数値・スライド枚数を確認する。
  - **レイアウト**: 座標計算を自分で見直し、カードのはみ出し・要素の重なりを潰す。
- 仕様の数値（FT8 のビット数など）は、原稿と参考 PDF を突き合わせて裏取りする。憶測で書かない。

## Git / 依存

- パッケージマネージャは **pnpm**（`packageManager` 参照）。依存は `pptxgenjs`。
- `.gitignore`: `node_modules/`、`build/`、**`*.pptx`（生成物なのでコミットしない。`build_slides.js` から再生成可能）**。
- コミット対象は原稿 `.md`・`build_slides.js`・`scripts/`・設定ファイル。生成された `.pptx` はコミットしない。
